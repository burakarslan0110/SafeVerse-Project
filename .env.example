# SafeVerse Environment Variables
# =============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all required values marked with [REQUIRED]
# 3. Update optional values as needed
# 4. Never commit .env file to version control
# =============================================================================

# =============================================================================
# DEPLOYMENT PLATFORM (Optional - for Coolify, Railway, Render, etc.)
# =============================================================================
# These variables are used by some deployment platforms for service discovery

# API Service Domain (e.g., api.yourdomain.com)
SERVICE_FQDN_API=

# Web Service Domain (e.g., yourdomain.com or www.yourdomain.com)
SERVICE_FQDN_WEB=

# API Service Full URL (e.g., https://api.yourdomain.com)
SERVICE_URL_API=

# Web Service Full URL (e.g., https://yourdomain.com)
SERVICE_URL_WEB=

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# [REQUIRED] API Base URL - Backend API endpoint
# IMPORTANT: Do NOT include /api suffix - it's added automatically
#
# Examples:
#   Local development:     http://localhost:8080
#   Local network:         http://192.168.1.100:8080
#   Production:            https://api.yourdomain.com
EXPO_PUBLIC_API_BASE_URL=

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================

# [REQUIRED] Database name
POSTGRES_DB=

# [REQUIRED] Database username
POSTGRES_USER=

# [REQUIRED] Database password
# SECURITY: Use a strong password (minimum 16 characters)
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=

# [REQUIRED] Database connection string for backend API
# Format: Host=hostname;Database=dbname;Username=user;Password=pass;Port=5432
# For Docker Compose, use "db" as hostname
# Example: Host=db;Database=safeverse;Username=postgres;Password=your_password;Port=5432
DATABASE_URL=

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================

# [REQUIRED] JWT Secret Key
# SECURITY: Must be at least 32 characters
# Generate with: openssl rand -base64 32
JWT_SECRET_KEY=

# [REQUIRED] JWT Issuer (e.g., SafeVerse, YourAppName)
JWT_ISSUER=

# [REQUIRED] JWT Audience (e.g., SafeVerseClient, YourAppClient)
JWT_AUDIENCE=

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# [REQUIRED in Production] Allowed frontend origins
# Add all domains that should access your API
# Example: https://yourdomain.com
CORS_ORIGIN_1=

# Additional allowed origins (optional)
CORS_ORIGIN_2=
CORS_ORIGIN_3=

# =============================================================================
# LLM/AI CONFIGURATION (for AI-powered room safety analysis)
# =============================================================================

# LLM Provider (default: fallback)
# Options:
#   fallback - Try HTTP endpoint first, fallback to Gemini (recommended)
#   http     - Use only HTTP endpoint (free, no API key needed)
#   gemini   - Use only Google Gemini (requires API key)
#   mock     - Use mock data (development/testing)
LLM_PROVIDER=

# HTTP LLM Endpoint (set your LLM service endpoint URL)
LLM_ENDPOINT=

# Google Gemini API Key (optional, recommended for fallback mode)
# Get free API key: https://aistudio.google.com/app/apikey
# Required if LLM_PROVIDER=gemini
GEMINI_API_KEY=

# Gemini Model (default if not set: gemini-2.0-flash-exp)
# Options: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash
GEMINI_MODEL=

# =============================================================================
# ENVIRONMENT
# =============================================================================

# ASP.NET Core Environment (default if not set: Production)
# Options: Development, Production
ASPNETCORE_ENVIRONMENT=

# Node.js Environment (default if not set: production)
# Options: development, production
NODE_ENV=
